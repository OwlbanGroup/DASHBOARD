services:
  - type: web
    name: owlban-group
    runtime: docker
    dockerfilePath: ./quickstart/python/Dockerfile.dev
    dockerContext: .
    plan: free
    region: oregon
    healthCheckPath: /api/health
